# baddle

Инструмент для ручных экспериментов с генерацией текста нейросетью.
Позволяет заглянуть внутрь процесса — не просто получить ответ, а управлять им пошагово.

Большинство LLM-интерфейсов — чёрный ящик: отправил промпт, получил текст.
**baddle** открывает этот ящик: можно идти токен за токеном, смотреть распределение вероятностей,
вбрасывать свои токены в середину генерации, сравнивать как один и тот же промпт
ведёт себя при разных настройках — и всё это в терминале или в браузере.

С флагом `--server` baddle использует нативный `llama-server` из llama.cpp,
который умеет обрабатывать **два промпта одновременно** на GPU — оба генерируются
параллельно и занимают примерно столько же времени, сколько один.
Без `--server` промпты обрабатываются по очереди, то есть в два раза дольше.

**[Установка и запуск →](SETUP.md)**

---

## Режимы

### `step` — пошаговая генерация

Модель генерирует **один токен за раз**. После каждого токена можно посмотреть
распределение вероятностей (`top`), вбросить произвольный текст (`inject`),
сменить температуру (`temp`) — и продолжить. Полный список команд в [SETUP.md](SETUP.md).

---

### `parallel` — два промпта одновременно

Два разных промпта генерируются параллельно. Результат — live split-screen,
оба потока обновляются в реальном времени.

```
┌─ Stream A  step 12 ────────────────┬─ Stream B  step 12 ──────────────┐
│ The future of open source AI is    │ The biggest risk of closed AI is  │
│ bright — communities around the    │ concentration of power in a few   │
│ world are building tools that...   │ corporations that can shape...    │
└────────────────────────────────────┴──────────────────────────────────┘
```

---

### `compare` — один промпт, два набора параметров

Один промпт, но **два разных конфига** (температура, top_k). Оба потока стартуют
из идентичных токенов и расходятся как только параметры дают разные выборки.
Интерфейс показывает точный шаг расхождения.

```
┌─ temp=0.0  top_k=1  step 8 ──────┬─ temp=1.1  top_k=40  step 8 [diverged @ step 3] ─┐
│ Scientists recently discovered   │ Scientists recently discovered                     │
│ that a new species of deep-sea   │ that music actually rewires the                    │
│ fish can survive in...           │ human brain in ways we never...                    │
└──────────────────────────────────┴────────────────────────────────────────────────────┘
```

---

## Что можно попробовать

**Step mode** — это главный исследовательский режим. Команда `top 10` показывает
распределение вероятностей: на фактическом вопросе вроде *«The capital of France is»*
модель почти детерминирована — один токен забирает 99% вероятности. На открытом
нарративе 10–20 токенов делят вероятность примерно поровну.

Команда `inject` позволяет вбросить произвольный текст в середину генерации.
Модель не ломается — она видит весь контекст через attention и строит правдоподобное
продолжение даже из нонсенса. Можно найти точку ветвления через `top`, выбрать
маловероятный путь через `inject`, и получить совершенно другой нарратив из того же промпта.

Температура — не просто «ползунок креативности». При temp=0 модель идёт по единственному
самому вероятному пути. При temp=1.4 токен с p=0.01 становится почти равновероятен
с токеном с p=0.40 — это видно через `top`.

**Parallel mode** — два промпта бок о бок. Интересно давать противоположные фреймы
(*«...will help humanity»* vs *«...will harm humanity»*) — модель с одинаковой
уверенностью обосновывает обе позиции. Или два одинаковых промпта с temp>0 —
расходятся уже на 1–3 токене, первый случайный токен задаёт всю траекторию.

**Compare mode** — один промпт, два набора параметров. Показывает точный шаг
расхождения — это токен, где распределение было достаточно широким чтобы разные
настройки дали разный выбор. На академическом тексте расхождение наступает поздно,
на художественном — сразу.

**Seed** — в parallel и compare режимах можно задать seed (`--seed 42` или через UI).
С одинаковым seed и параметрами модель даёт идентичный результат — удобно для
сравнения: меняешь один параметр и видишь именно его эффект, а не случайный шум.
Seed `-1` (по умолчанию) — случайный каждый раз.

---

## Сессии

В step-режиме все команды записываются. `save <файл>` сохраняет полный журнал:

```json
{
  "model": "Qwen3-8B-Q4_K_M.gguf",
  "prompt": "The scientist looked at the results and realized",
  "temp": 0.0,
  "events": [
    {"type": "step"},
    {"type": "inject", "text": " nothing"},
    {"type": "auto", "n": 30},
    {"type": "temp", "value": 0.9}
  ]
}
```

`load <файл>` воспроизводит сессию и оставляет модель в том же состоянии —
можно продолжать эксперимент с этой точки.
