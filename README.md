# baddle

> AHI Protocol — Augmented Human Intelligence.
> Не спрашивай у ИИ ответ — управляй процессом мышления.

**[English version](README_EN.md)** | **[Установка](SETUP.md)** | **[Видение](VISION.md)** | **[TODO](TODO.md)**

Граф мыслей с байесовской уверенностью, марковскими переходами и диалектическим синтезом. Задаёшь цель, нажимаешь Run — система автоматически исследует тему, проверяет гипотезы, синтезирует результат. Локально через llama.cpp или через API. Бесплатно, на квантизированной 8B модели.

---

## Как это работает

1. Задаёшь **цель** — "Доказать что прокрастинация полезна."
2. Нажимаешь **Run**
3. Система автоматически:
   - Генерирует гипотезы
   - Собирает evidence для каждой
   - Проверяет диалектически (тезис/антитезис/синтез через embedding centroid)
   - Задаёт себе вопросы ("а что я упустил?")
   - Коллапсирует верифицированные кластеры
   - Повторяет цикл
4. Получаешь **структурированное эссе** с аргументацией

---

## Операции с графом

### Think
Генерирует N коротких независимых мыслей по теме. Каждая мысль — отдельная попытка, не продолжение предыдущей. LLM автоматически классифицирует тип (hypothesis, fact, question, goal, action) и начальную уверенность.

### Elaborate
Углубление конкретной мысли. Раскрывает деталь, следствие или механизм. Дочерние узлы автоматически становятся **evidence** — LLM определяет, поддерживают они родительскую гипотезу (supports) или противоречат (contradicts).

### Expand
Ветвление от мысли в новом направлении. Не углубление, а другой ракурс на ту же тему. Создаёт siblings, а не children.

### Collapse
Синтез кластера мыслей в один узел. Из 5 отдельных идей — один связный абзац. Два режима: merge (удаляет исходные) и no-merge (добавляет синтез как дочерний, оригиналы остаются).

### Rephrase
Переформулировка мысли. Сохраняет суть, меняет форму. Полезно когда гипотеза слабая из-за плохой формулировки, а не из-за содержания.

### Verify (Smart DC)
Диалектическая проверка — ключевой механизм. Три параллельных генерации:
1. **Тезис** — сильнейший аргумент ЗА
2. **Антитезис** — сильнейший аргумент ПРОТИВ
3. **Нейтраль** — контекст и условия

Три текста -> три вектора в embedding-пространстве -> **центроид** (точка равновесия) -> синтез, максимально близкий к центроиду. Confidence = cosine similarity синтеза к центроиду. Принудительный антитезис ломает эхо-камеры.

### Walk
Random Walk по графу из выбранного узла. Использует transition probabilities на рёбрах. Показывает куда ведёт мысль — какие узлы чаще всего оказываются конечными точками.

---

## Математическая база

### Байесовская уверенность

Каждый узел имеет **confidence** (0-1):

```
P(H|E) = P(E|H) * P(H) / [P(E|H)*P(H) + P(E|~H)*(1-P(H))]
```

При добавлении evidence (supports/contradicts) confidence обновляется через Bayes. Alpha/Beta модель отслеживает баланс поддерживающих и противоречащих свидетельств.

### Марковские переходы

Каждое ребро имеет **transition_prob**, нормализованное по исходящим:
- Косинусное сходство эмбеддингов -> базовый вес
- Directed edges получают bonus x1.5
- Хебб: при навигации пользователя `tp += lr * (1 - tp)` — часто используемые пути усиливаются
- Детектор ловушек: узлы с высоким входящим и низким исходящим tp

### Временные связи

Узлы, созданные в пределах 5 минут, получают temporal links. Вес зависит от временной близости (0.3-0.6).

### Кластеризация

Connected components на рёбрах выше threshold. Используется для группового collapse, визуализации hull, и определения изолированных узлов.

---

## Цикл Tick — автоматическое мышление

`thinking.py` — движок автономного мышления. Моделирует то, как думает человек: сначала набросать идей, потом сгруппировать похожее, углубить каждую уникальную, усомниться, и повторить.

### Принцип

Четыре инструмента, четыре фазы. Каждая фаза отрабатывает до конца, прежде чем перейти к следующей:

```
GENERATE    мало идей?            -> Think (набрать массу)
MERGE       есть похожие?         -> Collapse (слить ДО углубления)
ELABORATE   есть голые?           -> Elaborate (добавить evidence)
DOUBT       есть непроверенные?   -> Smart DC (усомниться)
GENERATE+   всё проверено?        -> Think ("что упустил?", с контекстом)
SYNTHESIZE  ничего нового         -> Stable (финальное эссе)
```

### Почему этот порядок

1. **GENERATE** первый — нужна масса разных идей прежде чем работать с ними
2. **MERGE** до elaborate — не тратим работу на дубликаты. "8B дешевле" и "8B снижает затраты" сливаются в одну ноду ДО того, как обе получают evidence
3. **ELABORATE** до doubt — голая фраза "8B дешевле" проверяется хуже чем углублённая "8B дешевле → inference $0.001 vs $0.03 → consumer GPU достаточно"
4. **DOUBT** последний — проверяем то, что уже обосновано. SmartDC (тезис/антитезис/синтез) работает качественнее на развитых идеях
5. **GENERATE+** — после проверки всего ищем пробелы. Модель видит список уже верифицированных мыслей и генерирует недостающие аспекты

### Классификация

На каждом тике все ноды классифицируются:
- **bare** — гипотезы без evidence и ещё не verified (нужен elaborate)
- **unverified** — confidence < stable_threshold (нужен doubt)
- **verified** — confidence >= stable_threshold (готовы к merge или финалу)

Синтез-ноды (от collapse и SmartDC) не считаются bare — они уже обработаны.

### Сходимость

Цикл сходится естественно через три механизма:
- **Novelty check** — при генерации новых мыслей проверяется embedding similarity с существующими. Если > 0.92, мысль отклоняется. Когда модель исчерпала тему — сплошные отклонения → остановка
- **Lineage tracking** — каждая collapse-нода хранит `collapsed_from` (список source indices). Merge не сольёт ноду с её собственным предком — не перемалывает одно и то же
- **Phase completion** — каждая фаза полностью завершается перед следующей. Нет бесконечного чередования

### Infinite mode

Без лимита шагов. Цикл крутится пока модель не исчерпает тему:
```
generate 10 → merge 6 в 3 → elaborate 3 → doubt 3 → verified →
generate+ ("что упустил?") → 5 новых → merge → elaborate → doubt →
generate+ → novelty reject → EXHAUSTED → final synthesis
```

Чем сильнее модель — тем больше уникальных мыслей, тем длиннее цикл, тем глубже результат.

### Настройки Run

| Параметр | Default | Описание |
|----------|---------|----------|
| **Start ideas** | 5 | Сколько идей сгенерить перед началом цикла (3-20) |
| **Depth** | 2 | Раунды META "что упустил?" — больше = глубже исследование (1-10) |
| **Stable** | 0.8 | Порог confidence для verified статуса |
| **Collapse at** | 50 | Шаг принудительного collapse (при ограниченном режиме) |
| **Essay tokens** | 6000 | Максимум токенов финального эссе (500-30000) |
| **Infinite** | off | Без лимита шагов — цикл сам найдёт точку остановки |
| **Batched essay** | off | Пирамидальный синтез: секции → финальный текст |
| **Output** | essay | Формат финала: essay / brief / list / none |

### Force Collapse

С лимитом шагов: после N шагов — принудительное сжатие порциями по 5. Hard stop на 2N.

### Live-метрики

Во время Run в правом верхнем углу графа отображается оверлей:
- **Step / Phase** — текущий шаг и фаза цикла
- **Hyp / Verified / Avg** — количество гипотез, сколько верифицировано, средний confidence
- **Sparkline** — кривая сходимости (verified/total). Пила = новые идеи входят → проверяются → пик. Затухание пилы = сходимость

---

## Интерфейс

Светлая тема (Notion-style). Четыре режима: **Graph** (основной), **Chat**, **Step**, **Parallel**.

### Graph mode
- **Topic + Think/Add** — ввод темы, автоматическая генерация мыслей
- **Save/Load** — сохранение графов на сервер, автосохранение после каждого действия
- **Actions bar** — Select, Collapse, Link, Flow, Time, Layout, Auto, Run, Undo, Save, Reset
- **SVG визуализация** — drag, zoom, pan, цветовая кодировка по типу и уверенности
- **Detail panel** — heatmap по токенам, confidence, тип, alpha/beta, Walk, Verify
- **Контекстное меню** — Expand, Elaborate, Rephrase, Verify, Walk, Evidence, Chat, Edit, Delete
- **Generation Studio** — пакетные варианты для rephrase/elaborate/expand/collapse

### Chat mode
- Разговор с моделью, context sidebar (граф -> чат, чат -> граф)

### Step mode
- Токен за токеном, top-10 кандидатов с вероятностями, heatmap

### Parallel mode
- Два промпта параллельно, compare mode (один промпт, разные параметры)

---

## Настройки

- **Local / API / Hybrid** — генерация и embeddings раздельно
- **Embedding model** — отдельная модель для similarity и centroid (nomic-embed-text)
- **Heatmap** — настраиваемая шкала энтропии

> Qwen3-8B для генерации + nomic-embed-text для embeddings. Через LM Studio обе параллельно.

---

## Структура проекта

```
baddle/
  ui.py                    # точка входа (Flask)
  setup.py                 # установка
  src/
    main.py                # модель, сэмплинг, эмбеддинги
    thinking.py            # автономное мышление (tick, фазы, BFS)
    graph_logic.py         # логика графа (ноды, рёбра, Байес, SmartDC)
    graph_routes.py        # Flask роуты графа
    prompts.py             # системные промпты (EN/RU)
    chat.py                # чат режим
    step.py                # step режим
    parallel.py            # parallel режим
    api_backend.py         # API клиент (OpenAI-compatible)
    server_backend.py      # llama-server
  static/
    css/style.css          # Notion-style тема
    js/                    # 6 JS модулей (graph, chat, step, parallel, modes, settings)
  templates/index.html     # HTML разметка
  graphs/                  # сохранённые графы
  models/                  # GGUF модели
```

---

## Быстрый старт

```bash
git clone https://github.com/Volk-Arch/Baddle.git
cd Baddle
python setup.py        # установит зависимости + скачает llama-server
python ui.py           # откроет http://localhost:7860
```

Подробнее: [SETUP.md](SETUP.md)
