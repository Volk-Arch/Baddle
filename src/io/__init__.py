"""IO layer — Flask routes + state helpers.

Реализует ветвь H (W18 ontology Phase 4) — HTTP layer над substrate/process/
memory. Routes split по доменам в `src/io/routes/{chat, goals, activity, ...}.py`.
"""
