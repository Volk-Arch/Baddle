# TODO

> Концепция, архитектура и научные основы → [README](README.md)
>
> Этот файл — что осталось сделать, в порядке приоритета.

---

## v1: стабилизация

- [ ] **Тестировать текущий цикл** — autorun на 3-5 разных темах, проверить сходимость
- [ ] **Novelty check оптимизация** — 30 embedding API-вызовов = медленно. Варианты: batch, Jaccard → embedding двухступенчато

---

## Целевая архитектура

```python
def tick(graph, horizon):
    horizon.update(surprise, gradient, novelty)
    phase = horizon.select_phase()    # policy weights → выбор фазы
    params = horizon.to_llm_params()  # precision → temperature/top_k/top_p

    if phase == "generate":  return brainstorm(graph, params)
    if phase == "merge":     return collapse_similar(graph, params)
    if phase == "elaborate": return elaborate(graph, params)
    if phase == "doubt":     return smartdc(graph, params)
```

Один tick, один horizon, четыре функции. Режим = preset. Текущий if/else в thinking.py — промежуточное состояние.

---

## v1.5: CognitiveHorizon — адаптивные параметры

Критический путь (порядок реализации):

```
Шаг 1: horizon.py (класс)                          ~1 час
  └─ precision, policy_weights, context_frame
  └─ to_llm_params() → {temp, top_k, top_p}
  └─ update(surprise, gradient, novelty)
  └─ select_phase() → phase name
  └─ 4 состояния: EXPLORATION/EXECUTION/RECOVERY/INTEGRATION

Шаг 2: _graph_generate принимает params             ~30 мин
  └─ Сейчас temp/top_k хардкожены или из UI
  └─ Нужно: принимать из horizon.to_llm_params()
  └─ Одна точка входа — все LLM вызовы через неё

Шаг 3: tick использует horizon                      ~30 мин
  └─ Создать/загрузить horizon из graph state
  └─ horizon.select_phase() вместо if/else
  └─ Передать params в brainstorm/elaborate/smartdc/collapse

Шаг 4: обратная связь                               ~30 мин
  └─ После SmartDC: surprise = 1 - confidence
  └─ После любой фазы: gradient = +1/-1/0
  └─ novelty уже считается в novelty check
  └─ horizon.update() → precision/policy обновились

Шаг 5: presets в modes.py                           ~30 мин
  └─ HORIZON_PRESETS для каждого из 13 режимов
  └─ init_horizon(mode_id) → настроенный Horizon

Шаг 6: UI                                          ~30 мин
  └─ Метрики в Run-оверлее: precision, width, state
  └─ Опционально: слайдер precision
```

Итого ~3.5 часа до работающего Horizon для Исследование + Мозговой штурм.

### Детали реализации (справка)

## v1.5: CognitiveHorizon — адаптивный движок мышления

Динамический контроллер, который регулирует параметры генерации на основе обратной
связи. Не генерирует контент — управляет **как** генерировать. Сидит между tick() и
LLM-вызовами, адаптируя temperature/top_p/prompts по ходу цикла.

### Проблема сейчас

tick() использует **фиксированные** параметры: temp=0.9, top_k=40 на все фазы.
Generate, Doubt, Collapse — всё с одинаковой температурой. Это как ехать на одной
передаче: можно, но неэффективно.

### Решение: три параметра Horizon

```
Π (precision)       : 0.0–1.0. Уверенность в текущей модели.
                      Выше = уже фильтр гипотез = ниже температура.
Λ (policy_weights)  : {generate: 0.3, merge: 0.2, elaborate: 0.2, doubt: 0.3}
                      Веса фаз. Определяют что делать на текущем шаге.
Γ (context_frame)   : Активная рамка — промпт, правила, ограничения.
                      Переключается при высокой novelty.
```

### Правила обновления

```
После каждого шага tick:

1. Precision:
   P_new = clamp(P_old + α * (surprise - target_surprise), 0.0, 1.0)

   surprise = 1 - confidence_после_SmartDC   (или 1 - cosine(expected, actual))
   target_surprise = гиперпараметр по задаче (0.15–0.6)
   α = скорость адаптации (0.05–0.2)

   Если surprise > target → precision падает → температура растёт → шире ищем.
   Если surprise < target → precision растёт → температура падает → фокусируемся.

2. Policy Weights:
   W_i += β * gradient_i
   W = normalize(W)

   gradient = +1 если фаза дала хороший результат (confidence вырос),
              -1 если провал (confidence упал или novelty reject)
   β = шаг обновления (0.1–0.3)

   Фазы которые работают хорошо → их вес растёт → tick выбирает их чаще.

3. Context Frame:
   if novelty > threshold: switch_frame()

   novelty = 1 - max_similarity(new_output, last_N_outputs)
   Высокая novelty = модель выдала что-то непохожее на предыдущее → сменить рамку.
```

### Маппинг Precision → LLM параметры

```python
temperature    = clamp(1.0 - precision, 0.1, 1.5)
top_k          = clamp(10 + 90 * (1 - precision), 10, 100)
top_p          = clamp(0.5 + 0.5 * precision, 0.7, 0.95)
presence_pen   = 0.0 if precision > 0.6 else 0.5
```

| Precision | Temperature | Режим мышления |
|-----------|-------------|---------------|
| 0.3 | 0.7 | Широкий поиск, креативность (GENERATE, META) |
| 0.5 | 0.5 | Сбалансированный (ELABORATE, COLLAPSE) |
| 0.8 | 0.2 | Узкий фокус, точность (DOUBT/SmartDC) |
| 0.9 | 0.1 | Детерминированный (финальный синтез) |

### Четыре состояния Horizon

| Состояние | Precision | Когда в нашем цикле | Триггер |
|-----------|-----------|--------------------|---------| 
| **EXPLORATION** | 0.3–0.5 | GENERATE, META | Мало гипотез, высокая entropy |
| **EXECUTION** | 0.7–0.9 | DOUBT, финальный COLLAPSE | Есть фокус, проверяем |
| **RECOVERY** | 0.4→0.6 | После провала SmartDC, collapse rollback | Surprise spike |
| **INTEGRATION** | 0.5–0.6 | MERGE, ELABORATE, промежуточный COLLAPSE | Данные собраны, синтезируем |

Переходы автоматические: surprise spike → RECOVERY, всё verified → EXECUTION,
META round → EXPLORATION, merge → INTEGRATION.

### Presets для режимов (из modes.py)

```python
HORIZON_PRESETS = {
    "horizon": {  # Горизонт (исследование)
        "precision_init": 0.4,
        "policy_weights": {"generate": 0.3, "merge": 0.2, "elaborate": 0.2, "doubt": 0.3},
        "target_surprise": 0.3,
        "alpha": 0.1, "beta": 0.2,
    },
    "fan": {  # Веер (мозговой штурм)
        "precision_init": 0.3,
        "policy_weights": {"generate": 0.5, "merge": 0.1, "elaborate": 0.1, "doubt": 0.3},
        "target_surprise": 0.5,
        "alpha": 0.15, "beta": 0.25,
    },
    "tournament": {  # Турнир (выбор)
        "precision_init": 0.7,
        "policy_weights": {"generate": 0.1, "merge": 0.1, "elaborate": 0.3, "doubt": 0.5},
        "target_surprise": 0.15,
        "alpha": 0.05, "beta": 0.1,
    },
    "free": {  # Свободный (ручной)
        "precision_init": 0.5,
        "policy_weights": {"generate": 0.25, "merge": 0.25, "elaborate": 0.25, "doubt": 0.25},
        "target_surprise": 0.3,
        "alpha": 0.1, "beta": 0.15,
    },
}
```

### Интеграция в tick

```
[tick вызывается]
    ↓
[classify_nodes → bare/unverified/verified]
    ↓
[horizon.policy_weights определяют какую фазу выбрать]
    ↓
[horizon.to_llm_params() → {temp, top_p, presence_pen}]
    ↓
[LLM вызов с динамическими параметрами]
    ↓
[Результат → оценка surprise/gradient]
    ↓
[horizon.update(surprise, gradient, novelty)]
    ↓
[Параметры обновились → следующий tick]
```

Ключевое: **policy_weights могут заменить хардкоженный if/else порядок фаз**.
Вместо "if bare: elaborate; elif unverified: doubt" — выбираем фазу по весам,
веса адаптируются по обратной связи.

### Метрики для UI оверлея

```
width         = 1 / (precision + ε)     — широта поиска гипотез
focus_entropy = -Σ(w * log₂(w))         — разброс внимания по фазам
alignment     = cosine(policy, gradient) — насколько действия ведут к цели
precision     = текущая точность Horizon
mode          = EXPLORATION / EXECUTION / RECOVERY / INTEGRATION
```

Показывать рядом со спарклайном в Run-оверлее:
`Step 15 · EXECUTION · Π=0.78 · width=1.28 · 4/6 verified`

### Что реализовать

**Этап 1: Ядро (horizon.py)**
- [ ] Класс `CognitiveHorizon` с precision, policy_weights, context_frame
- [ ] `update(surprise, gradient, novelty)` — обновление трёх параметров
- [ ] `to_llm_params()` → `{temperature, top_p, presence_penalty}`
- [ ] `get_metrics()` → width, focus_entropy, alignment
- [ ] 4 состояния: EXPLORATION / EXECUTION / RECOVERY / INTEGRATION
- [ ] Автоматические переходы между состояниями по триггерам

**Этап 2: Presets (в modes.py)**
- [ ] `HORIZON_PRESETS` — по одному на каждый mode
- [ ] `init_horizon(mode_id)` — создание настроенного Horizon из preset

**Этап 3: Интеграция в tick**
- [ ] tick() создаёт/использует Horizon из graph state
- [ ] Перед LLM вызовом: `params = horizon.to_llm_params()`
- [ ] После результата: `horizon.update(surprise, gradient, novelty)`
- [ ] Policy weights влияют на выбор фазы (вместо или вместе с if/else)

**Этап 4: Интеграция в graph_routes**
- [ ] `_graph_generate()` принимает params из Horizon вместо фиксированных
- [ ] SmartDC передаёт свои params (высокий precision → низкая temp)
- [ ] Collapse передаёт свои params (средний precision)

**Этап 5: Сигналы обратной связи**
- [ ] `surprise` = 1 - confidence после SmartDC
- [ ] `gradient` = +1 если confidence вырос, -1 если упал, 0 если без изменений
- [ ] `novelty` = 1 - max_similarity(new_node, existing_nodes) — уже есть в novelty check

**Этап 6: UI**
- [ ] Horizon метрики в Run-оверлее: precision, width, mode, policy dominant
- [ ] Опционально: ручная калибровка precision через слайдер

### Что это даёт

- **Адаптивная температура** — generate широко, doubt узко, автоматически
- **RECOVERY** — surprise spike → расширить горизонт (вместо stagnation хака)
- **Выбор фазы по весам** — мягче чем if/else, адаптируется к задаче
- **Presets по режимам** — каждый из 13 modes = свой набор гиперпараметров
- **Прозрачность** — видно в UI как система "думает": широко ищет или фокусируется

---

## v1.6: Накачка (Pump) — поиск третьего элемента между мыслями

Новый примитив работы с узлами, параллельный Smart DC. Две мысли A и B не имеют прямой
связи. Накачка ищет **третий элемент C** — промежуточное понятие, которое связано с обеими.
C не обязательно похоже на A или B по смыслу: оно дополняет их до общего контекста.

### Суть

**Smart DC** принимает одну ноду и проверяет её через диалектику (тезис/антитезис/синтез → центроид → confidence).

**Накачка** принимает две ноды и ищет **третий элемент** (мост) через двустороннюю
экспансию: каждая нода генерирует вокруг себя облако ассоциаций. В какой-то момент
в одном из облаков (или в обоих) появляется концепт C, который связан и с A, и с B.
Это не "A встречается с B", это "найдено C, которое делает A и B связанными".

### Физическая аналогия

Частицы соединяются если сумма спинов = 360. A = 180, B = 90, вместе = 270. Не хватает.
Добавляем 90 (третий элемент C) → 180 + 90 + 90 = 360 → связь замкнулась.

Важно: **C не похож на A или на B**. 90 не похож на 180, и 90 не обязательно похож
на другие 90. Важна не похожесть, а то что C **дополняет недостающее** — связывает
концы, которые сами по себе разомкнуты.

Другой пример: A = "иммунная система", B = "корпоративные банкротства". Они не
похожи. Но третий элемент C = "адаптация к внешним угрозам" связан и с иммунитетом,
и с бизнесом. C — мост, потому что он относится к обоим, хотя сам по себе не похож
ни на "иммунитет", ни на "банкротство".

### Отличие от поиска пути (BFS / shortest path)

BFS находит *существующие* связи в текущем графе. Если пути нет — результат "нет
связи". Накачка наоборот: если пути нет — **генерирует** промежуточные узлы пока
не появится третий элемент C. Это не поиск, это **создание моста**.

### Алгоритм

```
Вход: нода A, нода B

Инициализация:
    cloud_A = {A} + (уже существующие соседи A в графе)
    cloud_B = {B} + (уже существующие соседи B в графе)

Цикл (max_iterations = 5):
    1. Расширить cloud_A:
       - elaborate: что следует из A? (механизмы, детали, причины)
       - expand: какие есть аспекты A? (разные углы рассмотрения)
       - свободные ассоциации с контекстом A
       → новые узлы добавляются в cloud_A
    2. Расширить cloud_B аналогично

    3. Поиск скрытого параметра (LLM-as-abstractor):
       Вызов LLM с промптом:
           "У меня две группы понятий, связанные с двумя разными идеями.

            Идея A: [text(A)]
            Облако контекста A:
              - [text(a_1)]
              - [text(a_2)]
              - ...

            Идея B: [text(B)]
            Облако контекста B:
              - [text(b_1)]
              - [text(b_2)]
              - ...

            Вопрос: какой скрытый параметр / ось / измерение связывает эти
            две идеи? Не пересказывай облака. Найди ось, на которой обе
            оказываются точками. Ответь одной короткой формулировкой."

       Модель возвращает текст C — скрытый параметр.

       Оценка confidence:
           centroid_A = mean(embeddings of cloud_A)
           centroid_B = mean(embeddings of cloud_B)
           sim_to_A = cosine(emb(C), centroid_A)
           sim_to_B = cosine(emb(C), centroid_B)
           confidence = min(sim_to_A, sim_to_B)

       Если confidence > 0.5 → мост найден.

    4. Если confidence < 0.5 — продолжаем цикл, расширяем облака дальше.
       С каждой итерацией у модели больше контекста для абстракции.

    5. Проверить исчерпание (novelty check):
       если новые узлы в обоих облаках начали повторяться (sim > 0.92
       с существующими) → модель исчерпала контекст → стоп

Выход:
    - success: { bridge_text: C, confidence: float,
                 cloud_a: [...], cloud_b: [...] }
    - failure: "мост не найден — A и B слишком далеки или модель исчерпала контекст"
```

### Ключевой момент: LLM как абстрактор, а не embedding similarity

Наивная версия искала бы ноду C с высокой cosine similarity к обоим облакам.
Это работает для **текстово похожих** случаев, но **упускает абстрактные связи**.

"Иммунная система" и "корпоративные банкротства" не имеют общих текстов в облаках.
Но LLM может подняться на уровень абстракции и вывести: *"адаптивность против
жёсткости в ответ на внешние угрозы"*. Этой формулировки **нет** ни в одном
облаке — она существует только как выведенная модель оси.

**Embedding нужен только для оценки confidence** найденного моста: насколько он
реально относится к обеим сторонам. Но сам поиск — через генеративную способность
LLM обобщать.

### Что важно понять

- **C не содержится в облаках.** Это новая абстракция, которую LLM выводит из
  обоих контекстов. Embedding similarity в поиске не участвует, только в оценке.
- **Облака = контекст для LLM**, а не база для поиска. Чем богаче контексты, тем
  точнее LLM найдёт общую ось.
- **Мост — это формулировка, не нода.** Её можно сохранить в граф как новую
  ноду с `collapsed_from: [A_id, B_id]` и прямыми связями к A и B.
- **Инкрементальная попытка.** Первая итерация может не дать моста (облака ещё
  малы, LLM не видит абстракции). Расширяем дальше и пробуем снова.

### Почему это мощно

- **LLM-as-abstractor** находит связи на уровне, недоступном чистым embeddings
- **Двусторонняя экспансия** даёт модели богатый контекст вместо голых двух фраз
- **Результат — текст**, сразу пригодный для чтения, сохранения и дальнейшей работы
- **Confidence на основе центроидов** — простая и честная метрика "ось реально
  общая"

### UI

- **Контекстное меню на ноде** → "Pump to..." → выбрать вторую ноду кликом
- **Визуализация**: два облака расширяются в реальном времени, при встрече
  мостовые узлы подсвечиваются, путь A → мост → B выделяется линией
- **Кнопка на результате**: "Save as synthesis" — создать collapse-ноду из моста,
  или "Verify bridge" — прогнать Smart DC на найденной связи

### Что нужно реализовать

- [ ] **`pump_logic.py`** — основной алгоритм двусторонней экспансии с overlap detection
- [ ] **Роут `/graph/pump`** — принимает `{node_a_idx, node_b_idx, max_iterations}`,
      возвращает путь + confidence или "not connected"
- [ ] **Расширение cloud'а** — переиспользовать elaborate/expand/think из graph_routes.py
- [ ] **Overlap detection** — эффективная проверка пересечения двух множеств embeddings
      (наивно O(n²), можно ускорить через FAISS или простой порог)
- [ ] **Stop conditions** — успех / max iterations / novelty exhausted
- [ ] **JS**: контекстное меню, выбор второй ноды (мод "pick target"), прогресс-индикатор
- [ ] **Визуализация** — два расширяющихся облака, подсветка моста
- [ ] **Интеграция с Smart DC** — опция "verify bridge" после нахождения

### Место в архитектуре

Накачка — **примитивная операция на узлах**, не режим. Наравне с:
- **Smart DC** (тезис/антитезис/синтез → confidence одной ноды)
- **Pump** (двусторонняя экспансия → мост между двумя нодами)

Может использоваться внутри режимов алгебры как подоперация:
- **Диспут**: накачка между противоположными позициями для поиска общей почвы
- **Турнир**: накачка между вариантами для выявления скрытых общих критериев
- **Разведка**: случайная накачка между далёкими узлами как способ серендипити

---

## v2: алгебра режимов

Реализация оставшихся 11 режимов из [README](README.md). Цель — один tick, 12 конфигов.

### Шаг 1: инфраструктура конфига

- [ ] **Mode config в goal-ноде** — dict с `(mode, primitive, strategy, goals, goal_type, fields)`:
  - `primitive`: `none / and / or / xor` (четыре примитива)
  - `strategy`: `unordered / seq / priority / balance` (для AND) или `comparative / dialectical` (для XOR) или `null`
  - `goal_type`: `finite / repeatable / open`
- [ ] **Goal-нода как structured object** — сейчас только text, нужны дополнительные поля
- [ ] **Stop condition framework** — функции `(graph_state) → bool | snapshot`. По типу цели:
  - finite: `confidence ≥ threshold` → RESOLVED
  - repeatable: `step_complete + trigger` → SCHEDULED
  - open: `diminishing_returns ИЛИ budget` → PARKED
- [ ] **Goal evaluation** — LLM-as-judge: "достигли цели или нет?", сравнение goal↔result

### Шаг 2: диспетчер tick

- [ ] **tick(config, graph)** — читает `primitive` + `strategy` и вызывает реализацию

Четыре примитива:
- [ ] **none** (Scout) — без целей, дивергентное блуждание
- [ ] **AND** — все цели должны быть verified
- [ ] **OR** — первая достигнутая завершает цикл
- [ ] **XOR** — выбрать ровно одну из множества

Стратегии обхода AND (четыре режима):
- [ ] **unordered** — Конструктор: любой порядок
- [ ] **SEQ** — Конвейер: по зависимостям
- [ ] **PRIORITY** — Каскад: по важности
- [ ] **BALANCE** — Весы: пропорциональная аллокация над нефинитными

Стратегии разрешения XOR (два режима):
- [ ] **comparative** — Турнир: сравнение независимых опций
- [ ] **dialectical** — Диспут: синтез противоречивых утверждений через SmartDC-on-graph

Варианты OR (по типу цели):
- [ ] **OR finite** — Гонка (первая цель завершает)
- [ ] **OR open** — Веер (уже работает как текущий цикл)

### Шаг 3: режимы single-goal по типу цели

- [ ] **Вектор** (finite) — один фокус, сходится к RESOLVED
- [ ] **Ритм** (repeatable) — heartbeat, snapshot evaluation, streak/trend
- [ ] **Горизонт** (open) — уже работает как текущий цикл

### Шаг 4: UI

- [ ] **Селектор режима** — 12 опций с подсказками
- [ ] **Динамическая форма** — поля ввода из `config.fields` под режим
- [ ] **Display целей** — goal-нода показывает структуру (список целей + оператор)
- [ ] **Snapshot для repeatable** — виджет streak/today/trend вместо sparkline сходимости

### Шаг 5: персистентность

- [ ] **State beyond session** — Ритм работает днями/неделями, Вектор — месяцами
- [ ] **History log** — timestamps, changes, confidence evolution
- [ ] **Автосохранение** — уже есть, нужна проверка для long-running режимов

---

## v3: источники данных

- [ ] **Доступ в интернет** — search / RAG, для фактчекинга в исследовательских режимах
- [ ] **Гибрид LLM + поиск** — LLM генерит гипотезу → поиск проверяет факты
- [ ] **Per-этап выбор модели** — local 8B для generate, API для doubt/essay
- [ ] **UI** — настройки источника per-режим и per-этап

---

## v4: мульти-граф и мета-граф

- [ ] **Множественные графы** — вкладки, отдельный save/load, теги/слои
- [ ] **Мета-граф** — отдельный граф связей между графами
- [ ] **Cross-graph edges** — `serendipity_engine`, ассоциации между задачами
- [ ] **JSONL storage** — `nodes.jsonl` + `edges.jsonl` + `meta.json`, lazy load для больших графов

---

## v5: автономность

- [ ] **Автономное блуждание** — ночной режим, целенаправленный обход, поиск мостов
- [ ] **`watchdog.py`** — проактивный помощник, уведомления по триггерам
- [ ] **Консолидация** — прунинг слабых веток, "забывание" как фича
- [ ] **Данные с девайсов** — HRV, сон, шаги → для режима Ритм

### Бесцелевое сознание (Default Mode Network для графа)

Запуск без цели пользователя — система развивается автономно, как сознание младенца.

**Концепция:**
Нейробиология: default mode network (DMN) активен когда человек НЕ думает целенаправленно.
Блуждание ума — не баг, а способ нахождения скрытых связей. Инсайты приходят когда
не думаешь о проблеме.

**Реализация:**
- Scout mode (0 целей) + infinite + автосохранение
- Seed: случайное слово, или последний граф, или внешний стимул
- Цикл: brainstorm → elaborate → doubt → merge → brainstorm от результатов merge
- Без goal → без стоп-условия по confidence. Стоп только по novelty exhaustion.
- Pump между случайными далёкими нодами = поиск скрытых мостов (DMN-like)
- Cron job: запуск каждые N часов. Граф растёт между сессиями.

**От младенца к взрослому:**
- Horizon precision drift: начинает с 0.2 (сфера, всё возможно),
  постепенно растёт с количеством verified нод (конус сужается, модель мира уплотняется)
- Cross-graph: выводы одной сессии → seed следующей
- Консолидация: prune слабых веток (забывание), усиление сильных (память)
- Результат: граф который "знает" тему не потому что ему сказали, а потому что
  сам исследовал

**Открытый вопрос:** может ли **смысл** появиться из бесцелевого блуждания?
Нейробиология говорит да — DMN порождает самореференцию ("я", "мои мысли"),
планирование будущего, переосмысление прошлого. Если граф достаточно плотный,
Pump между далёкими нодами может породить мета-ноды — обобщения которых никто не запрашивал.

---

## v6: мета-режим А→Б

- [ ] **Автоопределение режима** — по промпту/намерению пользователя, без ручного селектора
- [ ] **Декомпозиция цели** — разбивка сложной задачи на подграфы разных режимов

---

## v7: экосистема и полировка

- [ ] **Layout** — d3/dagre/ELK вместо плоской линии
- [ ] **Тесты** — unit + integration
- [ ] **Параллельные API-запросы** — threading, ускорение цикла
- [ ] **Timeline player** — ⏮▶⏸⏭ по timestamps
- [ ] **Экспорт** — PNG / SVG / markdown / Obsidian
- [ ] **EXE-установщик** — PyInstaller, ~15-20 MB
- [ ] **Graph Store** — маркетплейс графов знаний
- [ ] **Git Verify** — MR для знаний, review, рейтинги
- [ ] **Извлечение графа из текста** — статья → граф

---

## Сделано, не тестировано

- **Промпты разнообразия** — think-промпт перечисляет 10 измерений (economic, social, technical...), new_idea подсказывает конкретные варианты. Окно existing 5→10
- **Novelty threshold в UI** — поле в Run settings, default 0.92, прокидывается в `/graph/think`
- **API-only переход** — удалён llama_cpp, server_backend, step, parallel. Всё через OpenAI API
- **Settings modal** — упрощён до API URL / key / model / embedding / ctx
- **Chat упрощение** — убраны SSE/Continue/Stop, ответ приходит одним чанком
- **Ask** — контекстное меню + Studio + detail panel, передаёт текст ноды в промпт
- **Generation Studio modal** — восстановлен, с режимом Ask
- **Вычистка кода** — graphTick, temporal рёбра, autorun handlers, ~1200 строк убрано
- **settings.json** — автосоздание с дефолтами при первом запуске
- **modes.py** — структура 12 режимов (`MODES` dict), роут `/modes`, `get_mode()`, `list_modes()`
- **Mode selector в UI** — dropdown в toolbar, заполняется из `/modes`, передаёт mode при создании goal
- **Goal-нода хранит mode_id** — записывается при создании, tick() читает из goal
- **Полный путь данных** — UI → goal-нода → tick() → (пока только horizon)
- **Mode selector redesign** — первый элемент в toolbar, крупный, с tooltip режима. Placeholder поля ввода меняется под режим. Mode хранится в graph meta + goal-ноде
