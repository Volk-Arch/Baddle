# TODO & Ideas

## Концепция

Человек мыслит не линейно. Он набрасывает точки (факты, гипотезы, ассоциации),
проверяет можно ли их соединить, и если получается — углубляется. Если нет —
добирает новых точек или перестраивает связи.

Цикл: генерация мыслей → граф связей → кластеризация → коллапс → повтор.

---

## High priority

- [ ] Байесовская уверенность в связях (цвет рёбер по уверенности в связи) по энтропии
- [ ] Коллапс в длинный текст
- [ ] Зум в графовом режиме
- [ ] Топ и температура в режиме чата и графовом
- [ ] Извлечение графа из текста (например, из статьи)(llm graph transform)
- [ ] Рассуждение с чатом по графу
- [ ] Heatmap энтропии в графовом режиме (цвет узлов по уверенности генерации)
- [ ] Вынос модулей в отдельные файлы


## Medium priority

- [ ] Системные промпты по языку — graph.py и chat захардкожены на EN, при RU модель думает на английском
- [ ] Лучшая кластеризация — spectral clustering или DBSCAN вместо connected components
- [ ] Повторные/похожие мысли — фильтр по similarity к уже существующим
- [ ] Управление вниманием модели через указание точек внимания в тексте.
- [ ] Случайный шум в токены


## Low priority

- [ ] Автоматический прогон: список тем → генерация N мыслей по каждой → подсчёт метрик
- [ ] Метрики: количество кластеров, средняя уникальность мыслей, diversity score, скорость насыщения
- [ ] Сравнение моделей: одни и те же темы, разные GGUF — таблица с результатами
- [ ] Экспорт отчёта: JSON + markdown с графиками
- [ ] Оценка связности — метрика "готовности к коллапсу" для всего графа
- [ ] Варианты продолжения в step mode — генерация нескольких вариантов (3–5) следующих N токенов, каждый в отдельном окошке. Клик по варианту — применяет его. По сути visual branching: видишь куда модель может пойти и выбираешь путь.
- [ ] Batch benchmark — N промптов × M конфигов → CSV со скоростью и метриками.
- [ ] REST API для интеграций — документированные эндпоинты (/api/generate, /api/sample, /api/logits) со стабильным JSON-контрактом, чтобы baddle можно было использовать как бэкенд из скриптов, ноутбуков или других UI. Сейчас эндпоинты заточены под внутренний фронтенд.

---